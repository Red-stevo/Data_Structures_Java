package org.example;

public class Main {
    public static void main(String[] args) {
        ArrayListInJava arrayListInJava = new ArrayListInJava();

        //arrayListInJava.setBookModel();

      /* arrayListInJava.printBooks();

        arrayListInJava.intArrayList();*/

        //arrayListInJava.setBookModel();

        //arrayListInJava.listArray();


        //arrayListInJava.setArrayList();

        arrayListInJava.printList();
    }


    /*
     *           WHAT I NOTED WHEN WORKING WITH ARRAYLIST CLASS.
     *         +_____________________________________________________+
     *
     *1. In the ArrayList no actual data is stored.Only references are store.
     *       this means that when dealing with objects and a single instance in initialized,
     *       the single object reference is stored in the ArrayList in the ArrayList and if the values of the
     *       object are changed the values for both references are the same.
     *
     *2. In simple terms :- ArrayList store references, If we use the same instance to set values
     *     then same values will be set for all abject.
     *   It will seem as if you are replacing the values of the list.
     *
     *
     *       Finally,;
     * Avoid using the same instance of an object to set values to an ArrayList.
     *
     * */










}
